/* flashcards-expanded.js — stub vide. Les cartes sont dans mega-flashcards*.js */
const FLASHCARDS_EXPANDED = [];
if (typeof module !== 'undefined' && module.exports) { module.exports = { FLASHCARDS_EXPANDED }; }
