/* flashcards-memos.js — stub vide. Les cartes sont dans mega-flashcards*.js */
const FLASHCARDS_MEMOS = [];
if (typeof module !== 'undefined' && module.exports) { module.exports = { FLASHCARDS_MEMOS }; }
