/* flashcards.js — Ce fichier est un stub vide.
   Les flashcards sont désormais dans mega-flashcards.js et mega-flashcards-2..10.js
   Ne pas modifier ce fichier directement. */

const FLASHCARDS = [];

if (typeof module !== 'undefined' && module.exports) { module.exports = { FLASHCARDS }; }
